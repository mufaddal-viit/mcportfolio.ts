import { Button } from "../ui/button";
import { ShinyButton } from "../ui/shiny-button"

function Available() {
    return (
        <ShinyButton className="bg-accent-2 cursor-none">
            Available
            {/* </span> */}
        </ShinyButton >
    )
}
export default Available;